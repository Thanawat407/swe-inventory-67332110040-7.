# Sprint Retrospective: Sprint 1

## Velocity
- Story point ที่วางแผน: 8
- Story point ที่ทำสำเร็จ (Done): 8
- Velocity Sprint 1: 8 points
*(หมายเหตุ: US-01 ค้นหา/กรอง RGB Sync ประเมินไว้ 3 points และ US-02 ตัดสต็อกด้วย Serial Number ประเมินไว้ 5 points รวมส่งมอบผ่าน Acceptance Criteria ครบถ้วน)*

## เพดานงานที่ทำพร้อมกัน (WIP limit)
- เพดานที่ตั้งไว้ใน TEAM_CHARTER.md: 3 ใบ
- ชนเพดานกี่ครั้งใน sprint นี้: 1 ครั้ง
- เพดานที่จะใช้ใน sprint หน้า: 3 ใบ เพราะ เหมาะสมกับจำนวนสมาชิกที่เป็น Developer 3 คนในทีมพอดี ช่วยป้องกันไม่ให้คนในทีมเปิดงานใหม่ค้างไว้โดยไม่ช่วยเพื่อนรีวิว PR

## Start: สิ่งที่ควรเริ่มทำในรอบต่อไป (อย่างน้อย 2 ข้อ พร้อมเหตุผล)
- **เริ่มเขียน Automated Unit Test สำหรับ Edge Cases ทันทีหลังตกลง Spec:** เพื่อให้รันตรวจ Acceptance Criteria ได้อัตโนมัติ ไม่ต้องคอยกรอกเทสต์มือบน CLI ทุกรอบที่มีการ regenerate โค้ด
- **เริ่มกำหนด Interface/Abstraction ให้ชัดเจนตั้งแต่รอบแรก:** เพื่อป้องกันปัญหาการละเมิดหลัก SOLID (OCP & DIP) จนต้องมาเสียเวลารีแฟกเตอร์ระบบใหญ่ในช่วงท้ายสปรินต์

## Stop: สิ่งที่ควรหยุดทำ (อย่างน้อย 2 ข้อ พร้อมตัวอย่างที่เกิดจริงใน sprint นี้)
- **หยุดแก้ไขโค้ดด้วยมือเมื่อผลลัพธ์ไม่ตรงตามที่ต้องการ:** ตัวอย่างเช่น ตอนทดสอบเงื่อนไขแจ้งเตือนสต็อกต่ำแล้ว AI เขียนเป็น `<= threshold` แทนที่จะเป็น `< threshold` การแก้ที่ถูกต้องคือต้องไปเพิ่ม Scenario ใน `spec.md` ให้ชัดเจน ไม่ใช่ไปแก้โค้ดเอง
- **หยุดเปิด Branch ค้างไว้นานเกิน 1 วัน:** สมาชิกบางคนทำฟีเจอร์แชทค้างไว้จนเกิดความล่าช้าในการรวมโค้ด ควรแตก Task ย่อยให้เล็กลงแล้วเปิด PR ถี่ๆ เพื่อให้เพื่อนรีวิวได้ง่าย

## Continue: สิ่งที่ทำได้ดี ควรทำต่อ (อย่างน้อย 2 ข้อ)
- **การปฏิบัติตามกฎ Peer Code Review อย่างเคร่งครัด:** ทุก PR มีการตั้งคำถามเรื่อง Data Validation และ Edge Case ก่อน merge เสมอ และไม่มีใครกด merge PR ของตัวเอง
- **การใช้ .ai-rules.md ควบคุมขอบเขตของ AI:** กำหนดข้อห้ามเรื่อง Network Library (`smtplib`) และ Database ภายนอกได้ชัดเจน ทำให้โค้ดที่ AI สร้างขึ้นอยู่ในขอบเขต Out of Scope ของโปรเจกต์

## AI Commit Audit
PR ไหนที่ commit message จาก AI ต้องแก้มากที่สุด เพราะอะไร:
- **PR #2 (Feature: ตัดสต็อกหน้าร้านและแจ้งเตือนสต็อกต่ำ):**
  - ข้อความที่ AI ร่างมาเดิมคือ `update inventory.py and fix stock logic`
  - สิ่งที่ draft ของ AI ขาดไปคือ **"บริบทและเหตุผล"** ว่าทำไมถึงแก้ ไม่ได้บอกว่าเป็นการเพิ่มการตรวจสอบสถานะ `SOLD` เพื่อป้องกันการขายซ้ำ และไม่ได้ระบุเรื่องการแจ้งเตือนสต็อกต่ำตามเกณฑ์ Threshold ทีมจึงต้องแก้ใหม่เป็น `feat: implement sell_by_serial with sold-status validation and low stock alert` ให้ตรงกับ Git diff จริง

## Action Item สำหรับ Sprint ถัดไป (1-2 ข้อ พร้อมชื่อคนรับผิดชอบ)
| Action | เจ้าของ |
|---|---|
| ออกแบบ Data Structure และ Interface สำหรับ US-04 (RAM Kit) และ US-05 (Advanced Specs) ลงใน `spec.md` | (Ratchapong) |
| สร้าง Test Suite จำลองการตัดสต็อกแบบ Concurrent เพื่อทดสอบความเสถียรของ `items.json` | (Thanawat) |
